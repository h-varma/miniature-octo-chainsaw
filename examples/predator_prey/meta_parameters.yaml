model_name: 'predator_prey'

# noise generator

data_noise:
  scale: 0.05
  description: 'how much noise to add to the data - (scale x 100)%'

parameter_noise:
  scale: 1
  description: 'how much noise to add to the parameters - (scale x 100)%'

# IVP solver

t_end: 50

# one-parameter bifurcation diagram

bifurcation:
  type: 'hopf'

control_1:
  name: 'Ni'
  min_value: 10
  max_value: 1000
  step_size: 20
  description: 'homotopy parameter'

# two-parameter bifurcation diagram

control_2:
  name: 'delta'
  description: 'free parameter'

# parameter estimation problem

global_parameters:
  - 'Bc'
  - 'Bb'

residual:
  scale: 'linear'
  type: 'relative'